---
title: BOOT
summary: "BOOT.md用ワークスペース テンプレート"
read_when:
  - BOOT.md チェックリストを追加する場合
---

<div id="bootmd">
  # BOOT.md
</div>

OpenClaw が起動時に何を行うべきかについて、短く明示的な指示を追加してください（`hooks.internal.enabled` を有効にします）。
タスクがメッセージを送信する場合は、メッセージツールを使用し、その後は `NO_REPLY` で応答してください。
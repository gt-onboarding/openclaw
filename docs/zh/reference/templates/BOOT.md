---
title: BOOT
summary: "用于 BOOT.md 的工作区模板"
read_when:
  - 在添加 BOOT.md 清单时
---

<div id="bootmd">
  # BOOT.md
</div>

为 OpenClaw 在启动时应执行的操作添加简短且明确的指令（启用 `hooks.internal.enabled`）。
如果任务需要发送消息，先使用消息工具发送，然后返回 NO_REPLY。
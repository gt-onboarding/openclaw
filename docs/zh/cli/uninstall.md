---
title: 卸载
summary: "`openclaw uninstall` 的 CLI 参考（移除 Gateway 服务和本地数据）"
read_when:
  - 你想移除 Gateway 服务和/或本地状态数据
  - 你想先进行一次 dry-run（模拟运行）
---

<div id="openclaw-uninstall">
  # `openclaw uninstall`
</div>

卸载 Gateway 服务及本地数据（CLI 将被保留）。

```bash
openclaw uninstall
openclaw uninstall --all --yes
openclaw uninstall --dry-run
```

---
title: Dashboard（仪表盘）
summary: "用于 `openclaw dashboard` 的 CLI 参考（打开 Control UI）"
read_when:
  - 你想使用当前令牌打开 Control UI
  - 你想在不启动浏览器的情况下输出该 URL
---

<div id="openclaw-dashboard">
  # `openclaw dashboard`
</div>

使用你当前的身份认证打开 Control UI。

```bash
openclaw dashboard
openclaw dashboard --no-open
```
